[project]
name = "snn-grokking"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "torch",
    "snntorch",
    "numpy",
    "matplotlib",
    "tqdm",
    "einops",
]
